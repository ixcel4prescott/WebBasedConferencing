<?
class BridgesController extends AppController
{
 /* 
Possibly not used DC -2012/09/13
function index() 
  {}
  
  function activity($bridge=null)
  {
    $path = GRAPH_ROOT . DS . $bridge . '.png';
    
    if(preg_match('/^\w+$/', $bridge) && is_file($path)) {
      header('Content-Type: image/png');
      header('Content-Length: ' . filesize($path));
      readfile($path);
    }

    exit;
  }
  */
}
